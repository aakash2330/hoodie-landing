/** @type {import('next').NextConfig} */
const nextConfig = {
 images: {
        domains: ['images.unsplash.com','assets.aceternity.com', 's3-us-west-2.amazonaws.com']
    }
};

export default nextConfig;
